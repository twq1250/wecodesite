# 开放平台开发者控制台 - 项目规格文档

## 1. 项目概述

**项目名称**: 开放平台开发者控制台 (Feishu Developer Console)  
**项目版本**: 1.0.0  
**项目描述**: 一个基于 React + Ant Design 的开放平台管理后台，用于管理第三方应用的生命周期，包括应用创建、基础信息配置、权限管理、事件订阅、成员管理等核心功能。

## 2. 技术栈

### 前端框架
- **React**: ^18.2.0
- **React Router**: ^6.20.0 (路由管理)
- **Ant Design**: ^4.24.16 (UI 组件库)
- **@ant-design/icons**: ^6.1.1 (图标库)

### 构建工具
- **Vite**: ^5.0.0
- **@vitejs/plugin-react**: ^4.2.0
- **Less**: ^4.2.0 (CSS 预处理器)

### 样式方案
- Less 模块化样式 (`.m.less`)
- CSS 变量管理全局主题

## 3. 目录结构

```
weCodesite/
├── src/
│   ├── components/
│   │   ├── AppCard/
│   │   │   ├── AppCard.jsx          # 应用卡片组件
│   │   │   └── AppCard.m.less        # 应用卡片样式
│   │   ├── BindEamapModal/
│   │   │   └── BindEamapModal.jsx    # 绑定 EAMAP 弹窗组件
│   │   ├── BindEamapSelect/
│   │   │   └── BindEamapSelect.jsx  # EAMAP 选择器组件
│   │   ├── CreateAppModal/
│   │   │   ├── CreateAppModal.jsx   # 创建应用弹窗组件
│   │   │   └── CreateAppModal.m.less
│   │   └── Layout/
│   │       ├── AppInfoBar/
│   │       │   ├── AppInfoBar.jsx   # 应用信息栏组件
│   │       │   └── AppInfoBar.m.less
│   │       ├── Header/
│   │       │   ├── Header.jsx       # 顶部导航栏组件
│   │       │   └── Header.m.less
│   │       ├── Sidebar/
│   │       │   ├── Sidebar.jsx      # 侧边导航栏组件
│   │       │   └── Sidebar.m.less
│   │       ├── Layout.jsx           # 主布局组件
│   │       └── Layout.m.less
│   ├── pages/
│   │   ├── ApiManagement/
│   │   │   ├── ApiManagement.jsx    # API 管理页面
│   │   │   ├── ApiManagement.m.less
│   │   │   ├── ApiPermissionDrawer.jsx  # API 权限抽屉组件
│   │   │   ├── ApiPermissionDrawer.m.less
│   │   │   ├── mock.js              # API 管理 Mock 数据
│   │   │   ├── route.js             # 路由配置
│   │   │   └── thunk.js             # 异步操作
│   │   ├── AppList/
│   │   │   ├── AppList.jsx         # 应用列表页面
│   │   │   ├── AppList.m.less
│   │   │   ├── mock.js              # 应用列表 Mock 数据
│   │   │   ├── route.js
│   │   │   └── thunk.js             # 包含 fetchAppList, fetchDefaultIcons, fetchEamapOptions, createApp, fetchAppById, bindEamap
│   │   ├── BasicInfo/
│   │   │   ├── BasicInfo.jsx       # 基础信息页面
│   │   │   ├── BasicInfo.m.less
│   │   │   ├── mock.js
│   │   │   ├── route.js
│   │   │   └── thunk.js
│   │   ├── Capabilities/
│   │   │   ├── Capabilities.jsx    # 应用能力列表页面
│   │   │   ├── Capabilities.m.less
│   │   │   ├── route.js
│   │   │   └── thunk.js
│   │   ├── CapabilityDetail/
│   │   │   ├── CapabilityDetail.jsx # 能力详情页面
│   │   │   ├── CapabilityDetail.m.less
│   │   │   ├── route.js
│   │   │   └── thunk.js
│   │   ├── Events/
│   │   │   ├── Events.jsx           # 事件配置页面
│   │   │   ├── Events.m.less
│   │   │   ├── EventDrawer.jsx      # 添加事件抽屉组件
│   │   │   ├── EventDrawer.m.less
│   │   │   ├── mock.js
│   │   │   ├── route.js
│   │   │   └── thunk.js             # 包含 fetchEventList, fetchSubscriptionConfig, fetchAllEvents
│   │   ├── Members/
│   │   │   ├── Members.jsx          # 成员管理页面
│   │   │   ├── Members.m.less
│   │   │   ├── mock.js              # 包含 mockMembers, mockUsers, rolePermissions
│   │   │   ├── route.js
│   │   │   └── thunk.js
│   │   ├── OperationLog/
│   │   │   ├── OperationLog.jsx     # 操作日志页面
│   │   │   ├── OperationLog.m.less
│   │   │   ├── mock.js
│   │   │   ├── route.js
│   │   │   └── thunk.js             # 包含 fetchOperationLogList, fetchOperationLogFilters
│   │   └── VersionRelease/
│   │       ├── VersionRelease.jsx   # 版本发布页面
│   │       ├── VersionRelease.m.less
│   │       ├── VersionForm.jsx      # 版本表单页面
│   │       ├── mock.js              # 包含 mockVersions
│   │       ├── route.js
│   │       └── thunk.js
│   ├── styles/
│   │   └── global.m.less            # 全局样式和 CSS 变量
│   ├── App.jsx                      # 根组件和路由配置
│   └── main.jsx                    # 应用入口
├── index.html
├── jsconfig.json
├── package.json
└── vite.config.js
```

## 4. 全局样式规格

### CSS 变量定义

```less
:root {
  --primary-color: #0066ff;           // 主色调
  --primary-hover: #0052cc;           // 主色调悬停
  --primary-light: #e6f0ff;           // 主色调浅色
  --text-primary: #1f1f1f;           // 主要文字颜色
  --text-secondary: #5e5e5e;         // 次要文字颜色
  --text-tertiary: #8e8e8e;          // 第三级文字颜色
  --border-color: #e3e3e3;           // 边框颜色
  --bg-primary: #ffffff;             // 主要背景色
  --bg-secondary: #f7f7f7;           // 次要背景色
  --bg-hover: #f2f2f2;               // 悬停背景色
  --success-color: #0f9d58;          // 成功状态颜色
  --warning-color: #f4b400;          // 警告状态颜色
  --error-color: #db2828;            // 错误状态颜色
  --shadow-sm: 0 2px 4px rgba(0, 0, 0, 0.08);    // 小阴影
  --shadow-md: 0 4px 8px rgba(0, 0, 0, 0.12);    // 中阴影
  --radius-sm: 4px;                  // 小圆角
  --radius-md: 8px;                  // 中圆角
  --spacing-base: 16px;              // 基础间距
}
```

### 全局重置样式
- 清除默认 margin 和 padding
- 使用 `border-box` 盒模型
- 设置基础字体为 `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif`

## 5. 路由配置

### 路由结构 (App.jsx)

```
/                          → AppList (我的应用 - 首页)
/basic-info                → BasicInfo (凭证和基础信息)
/members                   → Members (成员管理)
/capabilities              → Capabilities (添加应用能力)
/capability-detail        → CapabilityDetail (能力详情)
/api-management            → ApiManagement (API管理)
/events                    → Events (事件配置)
/operation-log             → OperationLog (操作日志)
/version-release          → VersionRelease (版本发布)
/version-release/form     → VersionForm (版本表单)
```

### URL 参数规范
- `appId`: 应用 ID (例: `/basic-info?appId=1`)
- `caps`: 启用的应用能力列表，逗号分隔 (例: `?caps=bot,web`)
- `type`: 应用能力类型 (例: `?type=bot`)

## 6. 布局结构

### 主布局 (Layout.jsx)

```
┌────────────────────────────────────────────────────────────┐
│                     Header (64px)                          │
├────────────────────────────────────────────────────────────┤
│                     AppInfoBar (50px)                      │
├──────────────┬─────────────────────────────────────────────┤
│              │                                             │
│   Sidebar    │              Content Area                   │
│   (220px)    │                                             │
│              │                                             │
└──────────────┴─────────────────────────────────────────────┘
```

### 尺寸常量
- `HEADER_HEIGHT`: 64px
- `APP_INFO_BAR_HEIGHT`: 50px
- `SIDE_PADDING`: 16px
- `PADDING`: 24px
- `Sider width`: 220px

### 内容区域高度计算
- 首页: `calc(100vh - HEADER_HEIGHT - PADDING)`
- 详情页: `calc(100vh - HEADER_HEIGHT - APP_INFO_BAR_HEIGHT - PADDING)`

## 7. 组件详细规格

### 7.1 Header 组件

**文件路径**: `src/components/Layout/Header/Header.jsx`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────────┐
│ [Logo] 开放平台    开发文档                                     开发者 │
└─────────────────────────────────────────────────────────────────┘
```

#### 样式规格
- 高度: 64px
- 背景: `#fff`
- 内边距: `0 24px`
- 底部边框: `1px solid #f0f0f0`
- Logo 容器: 32x32px，圆角 6px，主色背景
- 字体大小: 品牌名 16px 粗体

#### 交互行为
- Logo 点击: 导航到首页 `/`
- "开发文档" 链接: 新窗口打开 `https://open.feishu.cn/`

---

### 7.2 AppInfoBar 组件

**文件路径**: `src/components/Layout/AppInfoBar/AppInfoBar.jsx`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────────┐
│ [🏠] │ 应用名称                                    │
│      │ 已绑定：xxx / 未绑定应用服务：[立即绑定]       │
└─────────────────────────────────────────────────────────────────┘
```

#### 样式规格
- 高度: 50px (紧凑布局)
- 背景: `#fff`
- Card 尺寸: `small`
- 标题字体: 14px 粗体
- 描述字体: 12px，颜色 `#8c8c8c`

#### 交互行为
- 首页图标点击: 导航到 `/`
- "立即绑定" 点击: 打开 `BindEamapModal` 弹窗

---

### 7.3 Sidebar 组件

**文件路径**: `src/components/Layout/Sidebar/Sidebar.jsx`

#### 布局结构
```
┌──────────────────────┐
│ 基础信息              │
│ ├─ 凭证和基础信息      │
│ └─ 成员管理           │
├──────────────────────┤
│ 应用能力              │
│ ├─ 添加应用能力       │
│ ├─ 机器人 (缩进)      │
│ ├─ 网页应用 (缩进)     │
│ └─ 小组件 (缩进)      │
├──────────────────────┤
│ 开发配置              │
│ ├─ API管理           │
│ └─ 事件配置          │
├──────────────────────┤
│ 运营监控              │
│ └─ 操作日志          │
├──────────────────────┤
│ 版本管理              │
│ └─ 版本发布          │
└──────────────────────┘
```

#### 动态菜单项
根据 URL 参数 `caps` 动态显示已启用的应用能力

#### 样式规格
- 宽度: 200px
- 分类标题: 12px，文字颜色 `#8e8e8e`，大写字母
- 菜单项: 10px 12px 内边距，悬停背景 `#f2f2f2`
- 激活状态: 背景 `#e6f0ff`，文字颜色 `#0066ff`
- 缩进菜单项: 左侧 36px 缩进

#### 交互行为
- 点击菜单项: 导航到对应页面，携带 `appId` 和 `caps` 参数
- 点击能力类型: 导航到能力详情页

---

### 7.4 AppCard 组件

**文件路径**: `src/components/AppCard/AppCard.jsx`

#### 布局结构
```
┌────────────────────────────────────────────────┐
│ [Icon]  应用名称                                 │
│        [状态标签]                               │
├────────────────────────────────────────────────┤
│ 所有者：xxx                                    │
│ 我的角色：xxx                                  │
│ 最新动态：xxx                                  │
└────────────────────────────────────────────────┘
```

#### 样式规格
- 容器: 白色背景，1px 边框 `#f0f0f0`，圆角 8px
- 图标容器: 54x54px，圆角 14px，灰色背景
- 内边距: 24px
- 标签颜色: 已绑定绿色，未绑定警告色橙色

#### 交互行为
- 点击整张卡片: 导航到 `/basic-info?appId={id}`

---

### 7.5 CreateAppModal 组件

**文件路径**: `src/components/CreateAppModal/CreateAppModal.jsx`

#### 布局结构
```
┌────────────────────────────────────────────────────────────────────┐
│                        创建应用                                      │
├────────────────────────────────────────────────────────────────────┤
│ 应用图标                                                            │
│ ┌──────────┐  上传图片按钮    默认图标网格 (emoji 选择)              │
│ │ [预览]   │                                                        │
│ └──────────┘                                                        │
│                                                                    │
│ 中文名称        [________________]                                  │
│ 英文名称        [________________]                                  │
│ 中文应用描述    [________________]                                  │
│                [________________]                                  │
│ 英文应用描述    [________________]                                  │
│                [________________]                                  │
│ 绑定到应用服务  [下拉选择________]                                  │
├────────────────────────────────────────────────────────────────────┤
│                               [取消]  [创建]                         │
└────────────────────────────────────────────────────────────────────┘
```

#### 样式规格
- 弹窗宽度: 600px
- 图标预览: 64x64px，圆角 8px
- 默认图标网格: 8列自适应

#### 交互行为
- 点击默认图标: 选择该 emoji 作为应用图标
- 上传图片: 支持拖拽或点击上传图片作为图标
- 提交验证: 必须选择或上传图标

---

### 7.6 BindEamapModal 组件

**文件路径**: `src/components/BindEamapModal/BindEamapModal.jsx`

#### 功能描述
用于将应用绑定到 EAMAP (企业应用管理平台)

---

### 7.7 BindEamapSelect 组件

**文件路径**: `src/components/BindEamapSelect/BindEamapSelect.jsx`

#### 功能描述
EAMAP 服务的下拉选择器组件

---

## 8. 页面详细规格

### 8.1 AppList 页面 (首页)

**文件路径**: `src/pages/AppList/AppList.jsx`

#### 布局结构
```
┌────────────────────────────────────────────────────────────────┐
│ 我的应用                                    [+ 创建应用]          │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│ ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│ │  AppCard 1   │  │  AppCard 2   │  │  AppCard 3   │           │
│ └──────────────┘  └──────────────┘  └──────────────┘           │
│                                                                │
│ ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│ │  AppCard 4   │  │  AppCard 5   │  │  AppCard 6   │           │
│ └──────────────┘  └──────────────┘  └──────────────┘           │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 样式规格
- 最大宽度: 960px
- 居中显示
- 卡片网格: 3列布局，12px 间距
- 标题: 20px 粗体

#### 数据加载
```javascript
// thunk.js
fetchAppList()        // 获取应用列表
fetchDefaultIcons()   // 获取默认图标列表
fetchEamapOptions()   // 获取 EAMAP 选项
createApp(values)     // 创建新应用
```

---

### 8.2 BasicInfo 页面

**文件路径**: `src/pages/BasicInfo/BasicInfo.jsx`

#### 布局结构
```
┌────────────────────────────────────────────────────────────────┐
│ 凭证和基础信息                                                   │
│ 管理应用凭证、安全配置和回调地址                                  │
├────────────────────────────────────────────────────────────────┤
│ 应用凭证                                                        │
│ ├─ APP ID:  xxxxxxxxxx                                         │
│ └─ APP Secret: ******** [👁] [📋]                              │
├────────────────────────────────────────────────────────────────┤
│ 基础信息                                              [编辑]     │
│ ├─ 应用图标: [emoji]                                           │
│ ├─ 中文名称: xxx                                               │
│ ├─ 英文名称: xxx                                               │
│ ├─ 应用中文描述: xxx                                           │
│ ├─ 应用英文描述: xxx                                           │
│ └─ 功能示意图: xxx                                             │
│                                              [取消] [保存]      │
├────────────────────────────────────────────────────────────────┤
│ 认证方式                                             [编辑]     │
│ ├─ (•) Cookie  ( ) 数字签名  ( ) SOAHeader  ( ) SOAURL         │
│                                              [取消] [保存]      │
└────────────────────────────────────────────────────────────────┘
```

#### 样式规格
- 表单标签宽度: 120px
- Card 标题: 15px 粗体
- 表单项底部边框分隔

#### 交互行为
- 编辑按钮: 切换编辑/查看模式
- 保存按钮: 验证并保存表单数据
- 取消按钮: 重置表单并退出编辑模式
- APP Secret 显示/隐藏切换
- APP Secret 复制到剪贴板

---

### 8.3 Capabilities 页面

**文件路径**: `src/pages/Capabilities/Capabilities.jsx`

#### 布局结构
```
┌────────────────────────────────────────────────────────────────┐
│ 添加应用能力                                                     │
│ 为应用开启对应的客户端能力，开启后需完成配置并发布应用才能生效      │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│ ┌──────────────────────┐  ┌──────────────────────┐            │
│ │ 🤖 机器人                    [✓ 已添加]               │  │
│ │     通过会话...         前往配置 →                │  │
│ └──────────────────────┘  └──────────────────────┘            │
│                                                                │
│ ┌──────────────────────┐  ┌──────────────────────┐            │
│ │ 🌐 网页应用                  [+ 添加]                  │  │
│ │     H5 开发...                                            │  │
│ └──────────────────────┘  └──────────────────────┘            │
│                                                                │
│ ┌──────────────────────┐  ┌──────────────────────┐            │
│ │ 📧 小程序    [不推荐]       [+ 添加]                  │  │
│ │     支持在小程序...                                        │  │
│ └──────────────────────┘  └──────────────────────┘            │
│                                                                │
│ ┌──────────────────────┐                                       │
│ │ 📱 小组件                   [+ 添加]                  │      │
│ │     将应用嵌入...                                        │  │
│ └──────────────────────┘                                       │
│                                                                │
├────────────────────────────────────────────────────────────────┤
│ 说明                                                            │
│ • 应用能力开启后，需完成必填项的配置并提交版本发布申请后，才能在线上生效 │
│ • 不同能力支持的配置项不同，请根据实际需求进行配置                 │
│ • 如需修改已开启的能力，请在对应能力详情页进行操作                 │
└────────────────────────────────────────────────────────────────┘
```

#### 能力类型配置
```javascript
const capabilityTypes = [
  { type: 'bot', name: '机器人', icon: 'RobotOutlined' },
  { type: 'web', name: '网页应用', icon: 'GlobalOutlined' },
  { type: 'miniapp', name: '小程序', icon: 'MailOutlined' },
  { type: 'widget', name: '小组件', icon: 'AppstoreOutlined' },
];
```

#### 样式规格
- 卡片网格: 2列布局
- 能力图标: 48x48px，圆形浅蓝色背景
- 已添加状态: 绿色勾选图标
- 不推荐标签: 橙色标签

#### 交互行为
- 点击已启用卡片: 导航到能力详情页
- 点击"添加"按钮: 打开确认弹窗，添加能力
- 添加后更新 URL 参数 `caps`

---

### 8.4 CapabilityDetail 页面

**文件路径**: `src/pages/CapabilityDetail/CapabilityDetail.jsx`

#### 布局结构
```
┌────────────────────────────────────────────────────────────────┐
│ ← 返回应用能力                                                   │
├────────────────────────────────────────────────────────────────┤
│ [Icon] 能力名称                                                  │
│        能力描述                                                  │
├────────────────────────────────────────────────────────────────┤
│ 基础配置                                                         │
│ ├─ 能力开关: [关 ● ]                                             │
│ ├─ [能力特定字段1]                                               │
│ ├─ [能力特定字段2]                                               │
│ └─ ...                                                          │
│                                                                │
│                              [取消]  [保存]                     │
└────────────────────────────────────────────────────────────────┘
```

#### 能力配置字段
```javascript
// bot
{ name: 'botName', label: '机器人名称', required: true }
{ name: 'botDesc', label: '机器人描述', required: false }

// web
{ name: 'homepageUrl', label: '桌面端主页地址', required: true }
{ name: 'mobileHomepageUrl', label: '移动端主页地址', required: false }
{ name: 'openMode', label: '桌面端主页打开方式', required: false }

// miniapp
{ name: 'appName', label: '小程序名称', required: true }
{ name: 'appDesc', label: '小程序描述', required: false }

// widget
{ name: 'widgetName', label: '小组件名称', required: true }
{ name: 'widgetDesc', label: '小组件描述', required: false }
```

---

### 8.5 ApiManagement 页面

**文件路径**: `src/pages/ApiManagement/ApiManagement.jsx`

#### 布局结构
```
┌────────────────────────────────────────────────────────────────┐
│ API管理                                      [+ 添加API]        │
│ 管理应用接口，配置API权限和调用参数                               │
├────────────────────────────────────────────────────────────────┤
│ 权限名称    │ codeName   │ 认证方式 │ 分类  │ 状态   │ 操作      │
│────────────┼────────────┼─────────┼───────┼────────┼───────────│
│ 日历权限    │ calendar   │ user    │ 日历  │ 已审核 │ [查看文档] │
│ 云文档权限  │ docx       │ user    │ 云文档 │ 审核中 │ [复制][撤回]│
└────────────────────────────────────────────────────────────────┘
```

#### 表格列配置
- 权限名称: 普通文本
- codeName: 代码样式显示
- 认证方式: 普通文本
- 分类: 普通文本
- 状态: 彩色标签 (已审核-绿色, 审核中-橙色, 已中止-红色)
- 操作: 根据状态显示不同按钮

#### 数据加载
```javascript
// thunk.js
fetchApiList()           // 获取 API 列表
fetchAvailableApis(type) // 获取可用 API (SOA/APIG)
fetchApiModules(type)    // 获取 API 模块分类
```

---

### 8.6 ApiPermissionDrawer 抽屉组件

**文件路径**: `src/pages/ApiManagement/ApiPermissionDrawer.jsx`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────────┐
│ 开通权限                                              [×]         │
├─────────────────────────────────────────────────────────────────┤
│ [SOA类型]  [APIG类型]                                            │
├─────────────┬───────────────────────────────────────────────────┤
│ 模块列表      │                                                   │
│─────────────│ 权限名称    │ 是否需要审核 │ 操作                   │
│ all         │─────────────┼──────────────┼─────────              │
│ 日历        │ 日历权限    │ 无需审核     │ [查看文档]              │
│ 云文档       │ 云文档权限  │ 需要审核     │ [查看文档]              │
│ 云空间       │ ...        │ ...         │ ...                    │
├─────────────┴───────────────────────────────────────────────────┤
│ [取消]                                              [确认开通权限]  │
└─────────────────────────────────────────────────────────────────┘
```

#### 样式规格
- 抽屉宽度: 900px
- Tab 切换: SOA 类型 / APIG 类型
- 左侧模块列表: 固定宽度
- 右侧 API 表格: 自适应宽度

---

### 8.7 Events 页面

**文件路径**: `src/pages/Events/Events.jsx`

#### 布局结构
```
┌────────────────────────────────────────────────────────────────┐
│ 事件配置                                                         │
│ 配置事件订阅和回调地址  [了解更多]                                 │
├────────────────────────────────────────────────────────────────┤
│ 订阅方式                                          [编辑]         │
│ 发送方式: (•) MQS平台订阅  ( ) 将事件发送至业务系统              │
│                                                                │
│ 说明文档: [查看]MQS内置消息队列使用规范                          │
│                                                                │
├────────────────────────────────────────────────────────────────┤
│ 已添加事件                                         [+ 添加事件]  │
│ ┌────────────────────────────────────────────────────────────┐ │
│ │ 事件名称    │ 事件类型 │ 所需权限  │ 操作                    │ │
│ │────────────┼─────────┼──────────┼─────────────────────────││
│ │ 应用创建事件 │ im       │ app      │ [删除]                  │ │
│ └────────────────────────────────────────────────────────────┘ │
│                                           共 X 条  [< 1 2 3 >]  │
└────────────────────────────────────────────────────────────────┘
```

#### 分页配置
```javascript
<Pagination
  current={currentPage}
  pageSize={pageSize}
  pageSizeOptions={[10, 20, 50]}
  showSizeChanger
  showQuickJumper
  showTotal={(total) => `共 ${total} 条`}
/>
```

---

### 8.8 EventDrawer 抽屉组件

**文件路径**: `src/pages/Events/EventDrawer.jsx`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────────┐
│ 添加事件                                              [×]         │
├─────────────────────────────────────────────────────────────────┤
│ 事件名称    │ 事件标识                                            │
│────────────┼────────────────────────────────────────────────────│
│ □ 应用创建  │ im.app.create                                       │
│ □ 消息接收  │ im.message.receive_v1                               │
├─────────────────────────────────────────────────────────────────┤
│                                       共 X 条  [< 1 2 3 >]       │
├─────────────────────────────────────────────────────────────────┤
│ [取消]                                              [确认添加]    │
└─────────────────────────────────────────────────────────────────┘
```

---

### 8.9 Members 页面

**文件路径**: `src/pages/Members/Members.jsx`

#### 布局结构
```
┌────────────────────────────────────────────────────────────────┐
│ 成员管理                                        [+ 添加成员]     │
│ 管理应用成员和角色权限                                          │
├────────────────────────────────────────────────────────────────┤
│ 姓名      │ 工号      │ 角色       │ 操作                      │
│───────────┼───────────┼────────────┼───────────────────────────│
│ 张三      │ EMP001    │ [管理员]   │ [删除]                    │
│ 李四      │ EMP002    │ [开发者]  │ [删除]                    │
└────────────────────────────────────────────────────────────────┘
```

#### 添加成员弹窗
```
┌────────────────────────────────────────────────────────────────┐
│ 添加成员                                              [×]        │
├────────────────────────────────────────────────────────────────┤
│ 人员搜索    [🔍 搜索姓名或工号__________]                        │
│             ┌─────────────────────────────┐                   │
│             │ 张三                        │                   │
│             │ EMP001 | zhangsan@company   │                   │
│             └─────────────────────────────┘                   │
│                                                                │
│ 角色         (•) 管理员    ( ) 开发者                           │
│             ┌────────────────────────────────┐                  │
│             │ 管理员权限说明                 │                  │
│             │ [权限标签1] [权限标签2] ...   │                  │
│             └────────────────────────────────┘                  │
├────────────────────────────────────────────────────────────────┤
│                                              [取消]  [确定]     │
└────────────────────────────────────────────────────────────────┘
```

---

### 8.10 OperationLog 页面

**文件路径**: `src/pages/OperationLog/OperationLog.jsx`

#### 布局结构
```
┌────────────────────────────────────────────────────────────────┐
│ 操作日志                                                         │
│ 查看用户操作记录                                                  │
├────────────────────────────────────────────────────────────────┤
│ 操作账号 [___________] 操作对象 [选择__▼] 操作时间 [选择范围__▼] │
│                                          [重置]  [查询]         │
├────────────────────────────────────────────────────────────────┤
│ 操作账号 │ 操作类型 │ 操作对象 │ 操作描述 │ 操作IP  │ 结果     │
│──────────┼──────────┼──────────┼──────────┼─────────┼──────────│
│ admin    │ 创建应用 │ 应用     │ 创建应用..│ 10.0... │ 成功     │
└────────────────────────────────────────────────────────────────┘
│ 最近180日数据                    共 X 条  [< 1 2 3 >]          │
└────────────────────────────────────────────────────────────────┘
```

#### 搜索表单配置
- 操作账号: Input 输入框
- 操作对象: Select 下拉选择
- 操作时间: RangePicker 范围选择器

---

### 8.11 VersionRelease 页面

**文件路径**: `src/pages/VersionRelease/VersionRelease.jsx`

#### 布局结构
```
┌────────────────────────────────────────────────────────────────┐
│ 版本发布                                         [+ 创建新版本]  │
│ 管理应用版本和发布历史                                           │
├────────────────────────────────────────────────────────────────┤
│ 版本号 │ 版本状态   │ 发布人 │ 审核通过时间 │ 操作              │
│────────┼───────────┼────────┼─────────────┼────────────────────│
│ 1.0.0  │ [已发布]  │ 张三   │ 2024-01-15  │ [查看]              │
│ 0.9.0  │ [审核中]  │ 李四   │ -           │ [查看]              │
│ 0.8.0  │ [未通过]  │ 王五   │ 2024-01-10  │ [查看] [删除]       │
└────────────────────────────────────────────────────────────────┘
```

#### 状态颜色映射
```javascript
const statusColorMap = {
  '已发布': 'green',
  '审核中': 'blue',
  '审核未通过': 'red',
};
```

---

### 8.12 VersionForm 页面

**文件路径**: `src/pages/VersionRelease/VersionForm.jsx`

#### 功能描述
版本发布的表单编辑页面

---

## 9. 主题配置

### Ant Design 主题定制 (App.jsx)

```javascript
<ConfigProvider
  theme={{
    token: {
      colorPrimary: '#0066ff',
      borderRadius: 6,
    },
  }}
>
```

---

## 10. Mock 数据结构

### 10.1 应用数据 (AppList/mock.js)
```javascript
{
  id: string,
  name: string,
  icon: string,          // emoji 或图片 URL
  owner: string,        // 邮箱格式
  role: string,         // '管理员' | '开发者'
  updateTime: string,   // 时间格式字符串
  eamap: string | null   // 绑定的 EAMAP 名称
}
```

### 10.2 成员数据 (Members/mock.js)
```javascript
// mockMembers
{
  id: number,
  name: string,
  employeeId: string,
  role: string,
  status: string
}

// mockUsers
{
  id: number,
  name: string,
  employeeId: string,
  email: string
}

// rolePermissions
{
  '管理员': [permission1, permission2, ...],
  '开发者': [permission1, permission2, ...]
}
```

### 10.3 版本数据 (VersionRelease/mock.js)
```javascript
{
  id: number,
  version: string,
  status: string,        // '已发布' | '审核中' | '审核未通过'
  publisher: string,
  approvedTime: string
}
```

---

## 11. 样式规范总结

### 页面结构
- 最大内容宽度: 960px (首页 AppList)
- 内边距: 24px
- 卡片间距: 16px
- 卡片内部间距: 24px

### 字体规范
- 页面标题: 18-20px 粗体
- 卡片标题: 15-16px 粗体
- 正文: 14px
- 辅助文字: 12-13px，颜色 `#8c8c8c` 或 `#8e8e8e`

### 颜色规范
- 主色: `#0066ff`
- 主色悬停: `#0052cc`
- 成功: `#52c41a`
- 警告: `#faad14`
- 错误: `#db2828`
- 背景色: `#f7f9fc` 到 `#f0f4f9` 渐变 (详情页)
- 白色卡片: `#fff`

### 圆角规范
- 按钮: 6px
- 卡片: 8px
- 图标容器: 8-14px

### 阴影规范
- 小阴影: `0 2px 4px rgba(0, 0, 0, 0.08)`
- 中阴影: `0 4px 8px rgba(0, 0, 0, 0.12)`

---

## 12. 交互规范

### 分页组件规范
所有列表页面使用独立的 Pagination 组件（非 Ant Design Table 内置分页）：
```javascript
<Pagination
  total={total}
  current={currentPage}
  pageSize={pageSize}
  pageSizeOptions={[10, 20, 50]}
  showSizeChanger
  showQuickJumper
  showTotal={(total) => `共 ${total} 条`}
  onChange={handlePageChange}
/>
```

### 弹窗规范
- Modal 组件使用 `destroyOnClose` 确保关闭时销毁
- 抽屉组件放在页面级别，方便状态管理

### 路由参数传递
- 使用 `useSearchParams` 获取 URL 参数
- 导航时携带 `appId` 和 `caps` 参数
- 保持参数一致性

---

## 13. 文件命名规范

- React 组件: PascalCase (如 `AppCard.jsx`)
- Less 样式文件: kebab-case + `.m.less` 后缀 (如 `AppCard.m.less`)
- JS/JSX 文件: PascalCase 或 camelCase
- Mock 数据: `mock.js`
- 路由配置: `route.js`
- 异步操作: `thunk.js`

---

## 14. 组件导出规范

```javascript
// 默认导出
export default ComponentName;

// 命名导出 (如工具函数)
export const helperFunction = () => { ... };
```

---

## 15. 注释规范

根据项目规范，代码中**不添加注释**。代码本身应该足够清晰，无需额外注释说明。

---

## 16. 依赖安装命令

```bash
npm install
```

### 开发环境启动
```bash
npm run dev
```

### 生产构建
```bash
npm run build
```

### 预览构建结果
```bash
npm run preview
```
