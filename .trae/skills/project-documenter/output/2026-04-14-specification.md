# 飞书开放平台应用管理后台 - 项目规格文档

## 一、项目概述

### 1.1 项目简介
本项目是一个飞书开放平台的应用管理后台，用于管理企业内部应用的开发、配置和发布流程。

### 1.2 技术栈
- **框架**: React 18
- **构建工具**: Vite
- **路由**: React Router
- **UI 组件库**: Ant Design 4
- **样式**: CSS Modules (less)
- **状态管理**: React Hooks (useState, useEffect, useMemo)

---

## 二、目录结构

```
/
├── src/                                    # 源代码目录
│   ├── components/                         # 公共组件
│   │   ├── AppCard/                        # 应用卡片组件
│   │   │   ├── AppCard.jsx                 # 卡片组件
│   │   │   └── AppCard.m.less              # 卡片样式
│   │   └── Layout/                         # 布局组件
│   │       ├── AppInfoBar/                 # 应用信息栏
│   │       │   ├── AppInfoBar.jsx
│   │       │   └── AppInfoBar.m.less
│   │       ├── Header/                     # 顶部导航
│   │       │   ├── Header.jsx
│   │       │   └── Header.m.less
│   │       ├── Sidebar/                    # 侧边栏导航
│   │       │   ├── Sidebar.jsx
│   │       │   └── Sidebar.m.less
│   │       ├── Layout.jsx                   # 根布局组件
│   │       └── Layout.m.less
│   ├── pages/                              # 页面组件
│   │   ├── ApiManagement/                  # API 管理 ⭐重点
│   │   │   ├── ApiManagement.jsx           # API管理主页面
│   │   │   ├── ApiManagement.m.less        # 样式
│   │   │   ├── ApiPermissionDrawer.jsx     # 开通权限Drawer
│   │   │   ├── ApiPermissionDrawer.m.less
│   │   │   ├── mock.js                    # 模拟数据
│   │   │   ├── route.js                    # 路由配置
│   │   │   └── thunk.js                   # 异步数据处理
│   │   ├── AppList/                        # 首页应用列表
│   │   │   ├── AppList.jsx                # 首页主页面
│   │   │   ├── AppList.m.less
│   │   │   ├── mock.js
│   │   │   ├── route.js
│   │   │   └── thunk.js
│   │   ├── BasicInfo/                     # 基础信息
│   │   │   ├── BasicInfo.jsx              # 凭证和基础信息页面
│   │   │   ├── BasicInfo.m.less
│   │   │   ├── mock.js
│   │   │   ├── route.js
│   │   │   └── thunk.js
│   │   ├── Capabilities/                  # 添加应用能力
│   │   │   ├── Capabilities.jsx            # 能力列表页面
│   │   │   ├── Capabilities.m.less
│   │   │   ├── route.js
│   │   │   └── thunk.js
│   │   ├── CapabilityDetail/              # 能力详情
│   │   │   ├── CapabilityDetail.jsx       # 能力配置页面
│   │   │   ├── CapabilityDetail.m.less
│   │   │   ├── route.js
│   │   │   └── thunk.js
│   │   ├── Events/                        # 事件配置 ⭐重点
│   │   │   ├── Events.jsx                 # 事件配置主页面
│   │   │   ├── Events.m.less
│   │   │   ├── EventDrawer.jsx            # 添加事件Drawer
│   │   │   ├── EventDrawer.m.less
│   │   │   ├── mock.js
│   │   │   ├── route.js
│   │   │   └── thunk.js
│   │   ├── Members/                       # 成员管理
│   │   │   ├── Members.jsx                # 成员管理页面
│   │   │   ├── Members.m.less
│   │   │   ├── mock.js
│   │   │   ├── route.js
│   │   │   └── thunk.js
│   │   └── VersionRelease/                # 版本发布
│   │       ├── VersionRelease.jsx         # 版本列表页面
│   │       ├── VersionRelease.m.less
│   │       ├── VersionForm.jsx           # 版本表单（创建/查看）
│   │       ├── mock.js
│   │       ├── route.js
│   │       └── thunk.js
│   ├── styles/
│   │   └── global.m.less                 # 全局样式变量
│   ├── App.jsx                            # 根组件
│   └── main.jsx                           # 入口文件
├── index.html                              # HTML 模板
├── package.json                            # 项目依赖配置
└── vite.config.js                         # Vite 构建配置
```

---

## 三、全局样式变量

```less
:root {
  --primary-color: #0066ff;          // 主色
  --primary-hover: #0052cc;          // 主色 hover
  --primary-light: #e6f0ff;          // 浅蓝背景
  --text-primary: #1f1f1f;           // 主要文字
  --text-secondary: #5e5e5e;         // 次要文字
  --text-tertiary: #8e8e8e;         // 三级文字
  --border-color: #e3e3e3;          // 边框色
  --bg-primary: #ffffff;             // 主背景
  --bg-secondary: #f7f7f7;           // 次背景
  --bg-hover: #f2f2f2;              // hover 背景
  --success-color: #0f9d58;          // 成功色
  --warning-color: #f4b400;          // 警告色
  --error-color: #db2828;            // 错误色
  --shadow-sm: 0 2px 4px rgba(0,0,0,0.08);
  --shadow-md: 0 4px 8px rgba(0,0,0,0.12);
  --radius-sm: 4px;
  --radius-md: 8px;
}
```

---

## 四、布局结构

### 4.1 整体布局

项目采用双模式布局设计：

**首页布局 (pathname === '/')**
```
┌─────────────────────────────────────────────────────────────┐
│                      Header (64px)                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                     <Outlet />                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**详情页布局 (pathname !== '/')**
```
┌─────────────────────────────────────────────────────────────┐
│                      Header (64px)                          │
├─────────────────────────────────────────────────────────────┤
│                   AppInfoBar (50px)                         │
├──────────────┬──────────────────────────────────────────────┤
│              │                                             │
│   Sidebar    │               <Outlet />                    │
│   (220px)    │                                             │
│              │                                             │
└──────────────┴─────────────────────────────────────────────┘
```

### 4.2 尺寸常量

| 常量 | 值 | 说明 |
|------|-----|------|
| HEADER_HEIGHT | 64px | 顶部导航高度 |
| APP_INFO_BAR_HEIGHT | 50px | 应用信息栏高度 |
| SIDE_PADDING | 16px | 侧边栏内边距 |
| PADDING | 24px | 内容区内边距 |
| SIDEBAR_WIDTH | 220px | 侧边栏宽度 |

### 4.3 高度计算公式

```javascript
// 详情页内容区高度
const contentAreaHeight = `calc(100vh - ${HEADER_HEIGHT}px - ${APP_INFO_BAR_HEIGHT}px - ${PADDING}px)`;
// 即: calc(100vh - 64px - 50px - 24px)

// 首页内容区高度
const contentAreaHeight = `calc(100vh - ${HEADER_HEIGHT}px - ${PADDING}px)`;
// 即: calc(100vh - 64px - 24px)
```

---

## 五、路由结构

| 页面 | 路由 | 说明 |
|------|------|------|
| AppList | `/` | 首页应用列表 |
| BasicInfo | `/basic-info?appId=x` | 凭证和基础信息 |
| Members | `/members?appId=x` | 成员管理 |
| Capabilities | `/capabilities?appId=x&caps=x,x` | 添加应用能力 |
| CapabilityDetail | `/capability-detail?appId=x&type=x&caps=x,x` | 能力详情 |
| ApiManagement | `/api-management?appId=x` | API管理 ⭐重点 |
| Events | `/events?appId=x` | 事件配置 ⭐重点 |
| VersionRelease | `/version-release?appId=x` | 版本发布 |
| VersionForm | `/version-release/form?appId=x` 或 `?appId=x&versionId=y` | 版本表单 |

---

## 六、组件文档

### 6.1 Header 组件

- **文件路径**: `/src/components/Layout/Header/Header.jsx`
- **用途**: 顶部导航栏

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│ [🔷 Logo] 开放平台      开发文档                    开发者  │
└─────────────────────────────────────────────────────────────┘
```

#### 样式规格
- **Colors**:
  - Logo 背景: `#0066ff`
  - 标题: `#1f1f1f`
  - 链接: `#5e5e5e`
- **Typography**: 标题 16px, font-weight: 600
- **Spacing**: 内边距 0 24px, Logo 与标题间距 8px
- **Dimensions**: Logo 32x32px, 圆角 6px

#### 交互
- **点击 Logo + 标题**: 导航到 `/`
- **点击"开发文档"**: 新窗口打开 `https://open.feishu.cn/`

---

### 6.2 AppInfoBar 组件

- **文件路径**: `/src/components/Layout/AppInfoBar/AppInfoBar.jsx`
- **用途**: 详情页顶部显示当前应用信息

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│ [🏠] │  测试应用                     │  ← AppInfoBar
│      │  已绑定：EAMAP-2024-001       │
└─────────────────────────────────────────────────────────────┘
```

#### 样式规格
- **Colors**: 应用名称 `#1f1f1f`, 绑定信息 `#00000073`
- **Typography**: 应用名称 14px font-weight: 600, 绑定信息 12px
- **Spacing**: 内边距 12px, 项间距 12px

#### 交互
- **点击🏠图标**: 导航到 `/`
- **点击"立即绑定"**: 触发绑定流程

---

### 6.3 Sidebar 组件

- **文件路径**: `/src/components/Layout/Sidebar/Sidebar.jsx`
- **用途**: 详情页左侧导航菜单

#### 布局结构
```
┌─────────────────────────┐
│ 基础信息                 │
│  ├ 🔑 凭证和基础信息     │  ← 选中态
│  └ 👥 成员管理           │
├─────────────────────────┤
│ 应用能力                 │
│  └ ⚡ 添加应用能力       │
├─────────────────────────┤
│ 开发配置                 │
│  ├ 🔗 API管理            │
│  └ 🔔 事件配置           │
├─────────────────────────┤
│ 版本管理                 │
│  └ 📦 版本发布           │
└─────────────────────────┘
```

#### 样式规格
- **Colors**: 分类标题 `#8e8e8e`, 菜单项 `#5e5e5e`, 选中项背景 `#e6f0ff`, 选中项文字 `#0066ff`
- **Typography**: 分类标题 12px uppercase, 菜单项 14px, 缩进菜单项 13px
- **Spacing**: 内边距 16px, 菜单项 10px 12px, 缩进菜单项左边距 36px
- **Dimensions**: 宽度 220px

#### 交互
- **点击菜单项**: 导航到对应页面，保留 `appId` 和 `caps` 参数
- **Hover**: 背景变为 `#f2f2f2`，文字变为 `#1f1f1f`
- **选中态**: 左侧蓝色指示条 + 蓝色文字 + 浅蓝背景

---

### 6.4 AppCard 组件

- **文件路径**: `/src/components/AppCard/AppCard.jsx`
- **用途**: 展示单个应用的信息卡片

#### 布局结构
```
┌───────────────────────────────┐
│ ┌────┐                        │
│ │图标 │  应用名称               │
│ │54x54│  [状态标签]             │
│ └────┘                        │
│                               │
│  所有者：   xxx                 │
│  我的角色： xxx                 │
│  最新动态： xxx                 │
└───────────────────────────────┘
```

#### 样式规格
- **Colors**: 背景 `#fff`, 边框 `#f0f0f0`, 所有者文字 `#1677ff`
- **Typography**: 应用名称 16px font-weight: 600
- **Spacing**: 内边距 24px, 图标与名称间距 16px, 元信息行间距 16px
- **Dimensions**: 图标 54x54px, 圆角 14px

#### 交互
- **点击卡片**: 导航到 `/basic-info?appId=${app.id}`

---

## 七、页面文档

### 7.1 AppList（首页应用列表）

- **文件路径**: `/src/pages/AppList/AppList.jsx`
- **用途**: 首页展示用户的所有应用列表
- **路由**: `/`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│                      Header (64px)                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│     ┌─────────────────────────────────────────────────┐     │
│     │   我的应用                        [创建应用]      │     │
│     │  ┌────────┐  ┌────────┐  ┌────────┐            │     │
│     │  │AppCard │  │AppCard │  │AppCard │            │     │  ← 3列网格布局
│     │  └────────┘  └────────┘  └────────┘            │     │
│     └─────────────────────────────────────────────────┘     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 样式规格
- **Colors**: 内容区背景 `linear-gradient(180deg, #f7f9fc 0%, #f0f4f9 100%)`, 按钮 Primary `#0066ff`
- **Typography**: 标题 20px font-weight: 600, 按钮 14px
- **Spacing**: 内容区 padding 24px, 卡片间距 12px
- **Dimensions**: 最大宽度 960px, 3列网格

#### 交互
- **点击"创建应用"**: 触发创建应用流程（当前为 UI 展示）
- **点击 AppCard**: 跳转到 `/basic-info?appId=${app.id}`

---

### 7.2 BasicInfo（凭证和基础信息）

- **文件路径**: `/src/pages/BasicInfo/BasicInfo.jsx`
- **用途**: 管理应用凭证、安全配置和回调地址
- **路由**: `/basic-info?appId=x`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  凭证和基础信息                                              │
│  管理应用凭证、安全配置和回调地址                              │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 应用凭证（无编辑按钮）                                 │   │
│  │  APP ID:     xxxxxxxxxxxxxxxxxxxx                    │   │
│  │  APP Secret: ************** [复制] [👁]              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 基础信息                               [编辑]         │   │
│  │  应用图标: [预览64x64] [上传]                         │   │
│  │  中文名称: xxx                                       │   │
│  │  英文名称: xxx                                       │   │
│  │  中文描述: xxxxxxxxxx                                │   │
│  │  英文描述: xxxxxxxxxx                                │   │
│  │  功能示意图: [--] 或 [图片预览]                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 认证方式                               [编辑]         │   │
│  │  ○ Cookie  ● 数字签名  ○ SOAHeader  ○ SOAURL        │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

#### 表单验证规则
| 字段 | 必填 | 说明 |
|------|------|------|
| 中文名称 | ✅ 必填 | |
| 英文名称 | ✅ 必填 | |
| 中文描述 | ❌ 非必填 | TextArea, 3行 |
| 英文描述 | ❌ 非必填 | TextArea, 3行 |
| 认证方式 | ✅ 必填 | Radio.Group 单选 |

#### 交互细节
- **APP Secret 交互**:
  - 复制图标：点击复制 APP Secret 到剪贴板
  - 眼睛图标：切换密码可见性（闭眼 `******`，睁眼显示真实值）
- **编辑模式**:
  - "应用凭证" 无编辑按钮（只读）
  - "基础信息" 和 "认证方式" 各有独立"编辑"按钮
  - 点击"编辑"后变为可编辑状态
  - 各区块编辑状态互不影响
- **功能示意图**: 无图片显示 `--`

---

### 7.3 Members（成员管理）

- **文件路径**: `/src/pages/Members/Members.jsx`
- **用途**: 管理应用成员和角色权限
- **路由**: `/members?appId=x`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  成员管理                              [添加成员]             │
│  管理应用成员和角色权限                                      │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐  │
│  │  姓名    │  工号      │  角色      │  操作            │  │
│  ├─────────────────────────────────────────────────────┤  │
│  │  张三    │  E001      │  [管理员]  │  [删除]          │  │
│  │  李四    │  E002      │  [开发者]  │  [删除]          │  │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

#### 样式规格
- **Colors**: 管理员标签 `#e6f4ea` 背景 `#0f9d58` 文字, 开发者标签 `#e8f0fe` 背景 `#0066ff` 文字
- **Typography**: 标题 18px, 表格内容 14px
- **Spacing**: 页面内边距 24px, 标题区间距 24px, 表格单元格 12px 16px

#### 交互
- **点击"添加成员"**: 打开 Modal 对话框
- **点击"删除"**: 弹出二次确认弹窗，确认后删除该成员
- **Modal 交互**:
  - 搜索框输入姓名或工号进行搜索
  - 选择用户后显示"已选择：xxx (工号)"
  - 选择角色（管理员/开发者，横向布局），下方显示对应权限说明
  - 点击"确定"添加成员，点击"取消"关闭

#### 添加成员 Modal 布局
```
┌─────────────────────────────────────────────────────────────┐
│  添加成员                                          [X]     │
├─────────────────────────────────────────────────────────────┤
│  人员搜索                                                │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ [🔍 搜索姓名或工号...]                               │  │
│  └─────────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  张三                                              │  │
│  │  E001 | zhangsan@company.com                       │  │
│  └─────────────────────────────────────────────────────┘  │
│  已选择：张三 (E001)                                      │
│                                                             │
│  角色                                                      │
│  ○ 管理员        ○ 开发者                                │
│                                                             │
│  角色权限说明                                              │
│  [查看应用信息] [调用API]                                   │
│                                                             │
│                                       [取消]  [确认添加]    │
└─────────────────────────────────────────────────────────────┘
```

---

### 7.4 Capabilities（添加应用能力）

- **文件路径**: `/src/pages/Capabilities/Capabilities.jsx`
- **用途**: 为应用开启对应的客户端能力
- **路由**: `/capabilities?appId=x&caps=x,x`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  添加应用能力                                                │
│  为应用开启对应的客户端能力，开启后需完成配置并发布应用才能生效    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────┐  ┌──────────────────────┐       │
│  │                      │  │                      │       │
│  │                      │  │                      │       │
│  │      🤖              │  │      🌐              │       │
│  │     机器人            │  │    网页应用           │       │
│  │                      │  │                      │       │
│  │  通过飞书会话与用户    │  │  H5开发，运行在       │       │
│  │  进行消息交互         │  │  飞书客户端内         │       │
│  │                      │  │                      │       │
│  │      ✓ 已添加         │  │      [添加]          │       │
│  └──────────────────────┘  └──────────────────────┘       │
│                                                             │
│  ┌──────────────────────┐  ┌──────────────────────┐       │
│  │                      │  │                      │       │
│  │                      │  │                      │       │
│  │      📧              │  │      📱              │       │
│  │    小程序[不推荐]     │  │     小组件            │       │
│  │                      │  │                      │       │
│  │  支持在小程序中实现    │  │  将应用嵌入到云文档   │       │
│  │  复杂交互             │  │  等飞书模块          │       │
│  │                      │  │                      │       │
│  │      [添加]          │  │      [添加]          │       │
│  └──────────────────────┘  └──────────────────────┘       │
│                                                             │
│  说明                                                       │
│  · 应用能力开启后，需完成必填项的配置并提交版本发布申请...     │
│  · 不同能力支持的配置项不同...                                │
│  · 如需修改已开启的能力，请在对应能力详情页进行操作            │
└─────────────────────────────────────────────────────────────┘
```

#### 能力类型
| 类型 | 名称 | 描述 | 特殊标识 |
|------|------|------|----------|
| bot | 机器人 | 通过飞书会话与用户进行消息交互 | - |
| web | 网页应用 | H5 开发，运行在飞书客户端内 | - |
| miniapp | 小程序 | 支持在小程序中实现复杂交互 | 橙标"不推荐" |
| widget | 小组件 | 将应用嵌入到云文档、多维表格等飞书模块 | - |

#### 交互
- **点击已启用卡片**: 跳转到能力详情页 `/capability-detail?appId=x&type=x`
- **点击"添加"按钮**:
  1. 打开确认添加 Modal
  2. 用户确认后，更新 URL 参数 `caps`
  3. 侧边栏"应用能力"分类下动态添加对应菜单项
  4. 能力卡片状态更新为"已添加"

---

### 7.5 CapabilityDetail（能力详情）

- **文件路径**: `/src/pages/CapabilityDetail/CapabilityDetail.jsx`
- **用途**: 查看和配置特定能力的详情
- **路由**: `/capability-detail?appId=x&type=x&caps=x,x`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  ← 返回应用能力                                              │
├─────────────────────────────────────────────────────────────┤
│  ┌─────┐                                                     │
│  │ 🤖  │  机器人                                              │
│  └─────┘  通过飞书会话与用户进行消息交互                         │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 基础配置                                              │   │
│  │                                                      │   │
│  │  能力开关：  [关] ────────────────────                │   │
│  │                                                      │   │
│  │  机器人名称： [请输入机器人名称]                       │   │
│  │  机器人描述： [请输入机器人描述]                       │   │
│  │                                                      │   │
│  │                          [保存] [取消]                │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

#### 各能力类型字段配置
| 能力类型 | 字段 | 必填 |
|----------|------|------|
| 机器人 | 机器人名称、机器人描述 | 仅名称必填 |
| 网页应用 | 桌面端主页地址、移动端主页地址、桌面端主页打开方式 | 仅桌面端主页地址必填 |
| 小程序 | 小程序名称、小程序描述 | 仅名称必填 |
| 小组件 | 小组件名称、小程序描述 | 仅名称必填 |

#### 交互
- **点击"← 返回应用能力"**: 跳转到 `/capabilities?appId=x&caps=x,x`
- **能力开关 Switch**: 开启/关闭能力
- **点击"保存"**: 验证表单，成功后显示 `message.success('保存成功')`
- **点击"取消"**: 返回应用能力页面

---

### 7.6 ApiManagement（API 管理）⭐ 重点页面

- **文件路径**: `/src/pages/ApiManagement/ApiManagement.jsx`
- **用途**: 管理应用接口，配置 API 权限和调用参数
- **路由**: `/api-management?appId=x`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  API管理                              [添加API]              │
│  管理应用接口，配置API权限和调用参数                            │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐  │
│  │权限名称 │ codeName │ 认证方式 │ 分类 │ 状态 │ 操作  │  │
│  ├─────────────────────────────────────────────────────┤  │
│  │获取用户信息 │ user.get │ SOA │用户管理│ [已审核]│...│  │
│  │发送消息 │ message.send │APIG│消息推送│ [审核中]│...│  │
│  │创建日历事件 │...│...│...│ [已中止] │...│               │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

#### 样式规格
- **Colors**: 已审核标签 `#e6f4ea` 背景 `#0f9d58` 文字, 审核中标签 `#fef7e0` 背景 `#b06000` 文字, 已中止标签 `#fce8e8` 背景 `#db2828` 文字
- **Typography**: 标题 18px, 表格内容 14px, codeName 13px monospace

#### 交互
- **点击"添加API"**: 打开权限开通 Drawer
- **点击"复制审批地址"**: 复制审核中 API 的审批地址
- **点击"撤回审核"**: 撤回该 API 的审核申请
- **点击"查看文档"**: 新窗口打开 API 文档

#### 已开通权限表格字段
| 字段 | 说明 |
|------|------|
| 权限名称 | API 名称 |
| codeName | API 代码名称（code 样式） |
| 认证方式 | SOA / APIG |
| 分类 | 用户管理、消息推送等 |
| 状态 | 已审核/审核中/已中止 |

#### 状态操作对应关系
| 状态 | 可用操作 |
|------|----------|
| 已审核 | 查看文档 |
| 审核中 | 复制审批地址、撤回审核、查看文档 |
| 已中止 | 查看文档 |

---

#### ApiPermissionDrawer（开通权限 Drawer）

- **文件路径**: `/src/pages/ApiManagement/ApiPermissionDrawer.jsx`
- **用途**: 开通 API 权限
- **宽度**: 900px，从右侧滑入

##### Drawer 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  开通权限                                            [X]    │
├─────────────────────────────────────────────────────────────┤
│  [SOA类型]  [APIG类型]                                      │
├────────────┬────────────────────────────────────────────────┤
│            │                                                │
│  所有      │  ┌──────────────────────────────────────────┐  │
│  用户管理   │  │ ☐ 权限名称      是否需要审核    操作     │  │
│  通讯录    │  ├──────────────────────────────────────────┤  │
│  组织架构   │  │ ☐ 获取用户详情    [需要审核]   [查看文档] │  │
│            │  │ ☐ 获取用户列表    [无需审核]   [查看文档] │  │
│            │  └──────────────────────────────────────────┘  │
│            │                                                │
│            │  共 12 条                                     │
│            │                              [< 1/2 >]         │
└────────────┴────────────────────────────────────────────────┘
│                                       [取消] [确认开通权限]  │
└─────────────────────────────────────────────────────────────┘
```

##### 交互
- **切换 Tabs (SOA/APIG)**: 加载对应类型的 API 列表，重置分类和分页
- **点击分类**: 筛选该分类下的 API，支持"所有"显示全部
- **勾选 API**: 多选框选择需要开通的 API
- **点击"查看文档"**: 新窗口打开 API 文档
- **分页操作**: 切换页码、每页条数（10/20/50）
- **点击"确认开通权限"**: 将选中的 API 添加到已开通列表
  - 如果 API 需要审核：状态为"审核中"
  - 如果 API 不需要审核：状态为"已审核"

##### API 类型分类
**SOA 类型**:
| 分类 | 说明 |
|------|------|
| 所有 | 全部 API |
| 用户管理 | 用户相关 API |
| 通讯录 | 通讯录相关 API |
| 组织架构 | 部门组织相关 API |

**APIG 类型**:
| 分类 | 说明 |
|------|------|
| 所有 | 全部 API |
| 云空间 | 云空间文件相关 API |
| 会议管理 | 会议相关 API |
| 公众号 | 公众号相关 API |
| 消息推送 | 消息发送相关 API |

---

### 7.7 Events（事件配置）⭐ 重点页面

- **文件路径**: `/src/pages/Events/Events.jsx`
- **用途**: 配置事件订阅和回调地址
- **路由**: `/events?appId=x`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  事件配置                                                    │
│  配置事件订阅和回调地址 [了解更多]                              │
├─────────────────────────────────────────────────────────────┤
│  订阅方式                                    [编辑]           │
│                                                             │
│  发送方式：  ○ MQS平台订阅  ○ 将事件发送至业务系统              │
│                                                             │
│  [当选择 MQS 时显示：查看 MQS内置消息队列使用规范]            │
│  [当选择 业务系统 时显示：请求地址输入框]                     │
│                                                             │
│  已添加事件                                [添加事件]         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 事件名称      │ 事件类型 │ 所需权限 │ 操作            │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 通讯录变化事件 │ 人员事件  │ 通讯录权限 │ [删除]       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                               [< 1/2 >]    │
└─────────────────────────────────────────────────────────────┘
```

#### 交互
- **点击"编辑"**: 开启编辑模式，显示保存/取消按钮
- **选择发送方式**:
  - MQS平台订阅：显示说明文档链接
  - 将事件发送至业务系统：显示请求地址输入框
- **点击"保存"**: 保存订阅配置
- **点击"取消"**: 取消编辑，恢复原状态
- **点击"删除"**: 弹出确认框，确认后删除该事件
- **点击"添加事件"**: 打开添加事件 Drawer
- **点击"了解更多"**: 跳转到事件文档页面

#### 订阅方式配置
| 方式 | 说明 | 条件显示 |
|------|------|----------|
| MQS平台订阅 | 使用飞书内置消息队列 | 显示说明文档链接 |
| 将事件发送至业务系统 | 自定义回调地址 | 显示请求地址输入框 |

---

#### EventDrawer（添加事件 Drawer）

- **文件路径**: `/src/pages/Events/EventDrawer.jsx`
- **用途**: 添加事件到已订阅列表
- **宽度**: 600px，从右侧滑入

##### Drawer 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  添加事件                                            [X]     │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ☐ 事件名称      │ 事件标识                          │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ ☐ 通讯录变化事件 │ im.contacts.difference           │   │
│  │ ☐ 用户进入事件   │ im.user.entrance                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  共 10 条                                                  │
│                                      [< 1/2 >]             │
└─────────────────────────────────────────────────────────────┘
│                                       [取消] [确认添加]      │
└─────────────────────────────────────────────────────────────┘
```

##### 交互
- **勾选事件**: 多选框选择需要添加的事件
- **分页操作**: 切换页码、每页条数（10/20/50）
- **点击"确认添加"**: 将选中的事件添加到已订阅列表
- **点击"取消"**: 关闭 Drawer，不做任何操作

---

### 7.8 VersionRelease（版本发布）

- **文件路径**: `/src/pages/VersionRelease/VersionRelease.jsx`
- **用途**: 管理应用版本和发布历史
- **路由**: `/version-release?appId=x`

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  版本发布                              [创建新版本]            │
│  管理应用版本和发布历史                                      │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 版本号 │ 版本状态 │ 发布人 │ 审核通过时间 │ 操作       │  │
│  ├─────────────────────────────────────────────────────┤  │
│  │ 1.0.0 │ [已发布] │ 张三   │ 2024-01-15  │ [查看]    │  │
│  │ 0.9.0 │ [审核中] │ 李四   │ --           │ [查看]    │  │
│  │ 0.8.0 │ [审核未通过] │ 王五 │ --  │ [查看] [删除] │  │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

#### 状态操作对应关系
| 状态 | 可用操作 |
|------|----------|
| 已发布 | 查看 |
| 审核中 | 查看 |
| 审核未通过 | 查看、删除 |

#### 交互
- **点击"创建新版本"**: 跳转到 `/version-release/form?appId=x`
- **点击"查看"**: 跳转到 `/version-release/form?appId=x&versionId=y`（查看模式）
- **点击"删除"**: 弹出确认框，确认后删除该版本（仅"审核未通过"状态可删除）

---

#### VersionForm（创建/查看版本表单）

- **文件路径**: `/src/pages/VersionRelease/VersionForm.jsx`
- **用途**: 创建新版本或查看版本详情
- **路由**: `/version-release/form?appId=x` 或 `/version-release/form?appId=x&versionId=y`

##### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  ← 返回版本列表                                              │
├─────────────────────────────────────────────────────────────┤
│  创建新版本 / 查看版本详情                                    │
│  填写版本信息并提交审核 / 查看版本信息和应用能力配置            │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 版本号                                              │   │
│  │ [1.0.0] / 1.0.0（查看模式为纯文本）                   │   │
│  │                                                     │   │
│  │ 版本描述                                            │   │
│  │ [文本域...] / 描述内容（查看模式为纯文本）             │   │
│  │                                                     │   │
│  │ ─────────────────────────────────────────────      │   │
│  │                                                     │   │
│  │ 当前添加的应用能力                                    │   │
│  │ [机器人标签] [网页应用标签]                           │   │
│  │                                                     │   │
│  │                          [提交审核] [取消]           │   │
│  │                          或 [完成]                   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

##### 表单字段
| 字段 | 必填 | 说明 |
|------|------|------|
| 版本号 | ✅ 必填 | 格式如 1.0.0 |
| 版本描述 | ✅ 必填 | TextArea，4行 |

##### 交互
- **点击"← 返回版本列表"**: 返回版本列表页
- **查看模式**: 表单字段为纯文本显示，底部显示"完成"按钮
- **创建模式**: 表单字段可编辑，底部显示"提交审核"和"取消"按钮
- **点击"提交审核"**: 验证表单，保存版本信息
- **点击"取消"**: 返回版本列表页
- **点击"完成"**: 返回版本列表页

---

## 八、重点页面详细说明

### 8.1 API 管理页 ⭐

API 管理是本项目的重点页面之一，主要功能包括：

#### 核心功能
1. **已开通 API 列表展示**
   - 展示已开通的 API 权限
   - 显示 API 名称、codeName、认证方式、分类、状态
   - 支持查看文档、复制审批地址、撤回审核等操作

2. **开通权限 Drawer**
   - 支持 SOA 和 APIG 两种认证类型切换
   - 左侧分类筛选（所有、用户管理、通讯录等）
   - 右侧 API 列表，支持多选
   - 分页功能（10/20/50条每页）
   - 显示是否需要审核标签

#### 数据结构
```javascript
// 已开通 API
{
  id: number,
  name: string,           // 权限名称
  codeName: string,      // codeName
  auth: string,          // 认证方式 (SOA/APIG)
  category: string,      // 分类
  status: string         // 状态 (已审核/审核中/已中止)
}

// 可开通 API
{
  id: number,
  name: string,
  codeName: string,
  auth: string,
  category: string,
  needReview: boolean,    // 是否需要审核
  docUrl: string          // 文档链接
}
```

---

### 8.2 事件配置页 ⭐

事件配置是本项目的另一个重点页面，主要功能包括：

#### 核心功能
1. **订阅方式配置**
   - 支持 MQS 平台订阅和业务系统回调两种方式
   - MQS 方式显示说明文档链接
   - 业务系统方式显示请求地址输入框
   - 编辑/保存/取消操作

2. **已添加事件列表**
   - 分页展示已订阅的事件
   - 显示事件名称、事件类型、所需权限
   - 支持删除操作（需二次确认）

3. **添加事件 Drawer**
   - 展示所有可用事件
   - 支持多选添加
   - 分页功能

#### 数据结构
```javascript
// 订阅配置
{
  method: 'mqs' | 'business',  // 订阅方式
  callbackUrl: string          // 回调地址（仅 business 方式）
}

// 事件
{
  id: number,
  name: string,        // 事件名称
  event: string,        // 事件标识
  eventType: string,    // 事件类型
  permission: string,    // 所需权限
  status: string        // 状态
}
```

---

## 九、总结

本项目是一个功能完整的飞书开放平台应用管理后台，包含以下核心模块：

1. **应用管理**: 首页展示应用列表，应用卡片点击进入详情
2. **基础信息**: 应用凭证管理、中英文信息配置、认证方式选择
3. **成员管理**: 添加/删除应用成员，角色权限配置
4. **应用能力**: 机器人、网页应用、小程序、小组件的添加和配置
5. **API 管理** ⭐: API 权限开通、管理，支持 SOA/APIG 两种认证类型
6. **事件配置** ⭐: 事件订阅配置，支持 MQS 和业务系统两种回调方式
7. **版本发布**: 版本创建、查看、删除管理

项目采用 React + Ant Design 4 技术栈，布局结构清晰，交互体验流畅，代码模块化程度高，便于维护和扩展。
